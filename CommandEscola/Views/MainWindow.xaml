<Window x:Class="CommandEscola.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CommandEscola.ViewModels"
        Title="Cadastro de Alunos" Height="400" Width="600">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Entrada -->
        <TextBox Grid.Row="0"
                 Text="{Binding NomeAluno, UpdateSourceTrigger=PropertyChanged}"
                 Height="30"
                 Margin="0 0 0 10"/>

        <!-- Botões -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Adicionar"
                    Command="{Binding AdicionarCommand}"
                    Width="100"
                    Margin="0 0 10 0"/>

            <Button Content="Remover"
                    Command="{Binding RemoverCommand}"
                    Width="100"/>
        </StackPanel>

        <!-- Lista -->
        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Alunos}"
                 SelectedItem="{Binding AlunoSelecionado}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Nome}" FontSize="16"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
